# golang, python3, nodejs, nvm, yarn, npm

# exa, bat, fzf, zsh, neovim, alacritty, httpie, tmux, tldr

# kubectl, docker, helm, virtualenvwrapper, ipython, gcloud, sdm, docker-compose, ngrok, aws-cli, terraform

# slack, telegram, zoom

# Linux only:
# SafeEyes (http://slgobinath.github.io/SafeEyes), CopyQ
